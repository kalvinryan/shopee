<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shopee">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shopee">
        <activity android:name=".views.Cart_product"></activity>
        <activity android:name=".views.Search_product" />
        <activity android:name=".views.Detail_product" />
        <activity android:name=".views.AddProduct" />
        <activity android:name=".views.kemas" />
        <activity android:name=".views.kirim" />
        <activity android:name=".views.bayar" />
        <activity android:name=".views.Home" />
        <activity android:name=".views.daftar" />
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>